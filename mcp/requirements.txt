# GTD MCP Server Requirements

# Core MCP SDK
mcp>=0.9.0

# Optional: RabbitMQ for background processing
pika>=1.3.0

# Google Health/Fitness integration (optional)
# Install with: pip install google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# Standard library should cover the rest
# (json, pathlib, datetime, etc. are built-in)

