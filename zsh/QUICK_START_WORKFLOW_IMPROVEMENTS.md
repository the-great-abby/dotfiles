# Quick Start: Workflow Improvements

## 🚀 5-Minute Setup

### Step 1: Add Aliases (2 minutes)

Add this line to your `~/.zshrc`:

```bash
source ~/code/dotfiles/zsh/gtd-aliases.zsh
```

Or copy the aliases directly into your `.zshrc`.

### Step 2: Reload Shell (10 seconds)

```bash
source ~/.zshrc
```

### Step 3: Test It (30 seconds)

```bash
# Test aliases
now          # See what to do
status       # Check system status
c "test"     # Quick capture
```

## ✅ You're Done!

Now you have:
- ✅ Quick aliases (`c`, `p`, `t`, `w`, `log`, `idea`, `task`)
- ✅ Quick routines (`morning`, `evening`, `now`)
- ✅ Context switching (`work`, `home`, `calls`)
- ✅ Navigation shortcuts (`inbox`, `projects`, `brain`)

## 📋 Daily Usage

### Morning
```bash
morning      # Start your day
```

### During Day
```bash
c "my task"  # Quick capture
i "my idea"  # Quick idea
log "note"   # Quick log
now          # What should I do?
```

### Evening
```bash
evening      # Wrap up your day
```

## 🎯 Most Used Commands

| Command | What It Does |
|---------|-------------|
| `c "task"` | Capture task |
| `i "idea"` | Capture idea |
| `log "note"` | Log entry |
| `p` | Process inbox |
| `t` | List tasks |
| `w` | Open wizard |
| `now` | What to do now |
| `status` | System status |

## 💡 Pro Tips

1. **Use `now` when stuck** - It shows what to focus on
2. **Use `c` for everything** - It's the fastest capture
3. **Use `morning`/`evening`** - Automate your routines
4. **Use `work`/`home`** - Switch contexts quickly

## 📚 Full Guide

See `WORKFLOW_IMPROVEMENTS.md` for complete details.



